{{#resume.basics}}
<section>
    <div class="row centre spread">
        {{#if profiles}}
            {{#each profiles}}
                {{> social}}
            {{/each}}
        {{/if}}
    </div>
</section>
{{/resume.basics}}
