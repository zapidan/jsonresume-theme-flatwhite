{{#resume.basics}}
<section class="profiles no-print">
    <div class="row centre spread">
        {{#if profiles}}
            {{#each profiles}}
                {{> social}}
            {{/each}}
        {{/if}}
    </div>
</section>
{{/resume.basics}}
